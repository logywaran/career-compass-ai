# ============================================
# Career Compass - Test Configuration
# ============================================
# Activated automatically during test execution

# Test Server Configuration
server.port=8080

# Dummy API Key for Tests (bypasses validation)
groq.api.key=test-dummy-key-for-unit-tests

# Test Logging - Less verbose
logging.level.org.springframework.web=WARN
logging.level.com.careercompass=INFO

# Disable banner in tests
spring.main.banner-mode=off

# JSON Configuration
spring.jackson.serialization.INDENT_OUTPUT=false
